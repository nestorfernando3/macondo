node_modules/
dist/
.DS_Store
internal/audio/deepgram.key
# Configuración local de la herramienta de desarrollo. Sus reglas de permisos repiten la
# llave de Deepgram en claro: es la misma que este archivo mantiene fuera del control de
# versiones, así que la carpeta entera queda fuera. Nunca se publica.
.commandcode/
# Estado local de here.now (enlace y token de reclamo del sitio publicado): nunca se publica.
.herenow/
# Copia de consulta de la novela: se queda en el equipo, fuera del repositorio público.
*.epub
